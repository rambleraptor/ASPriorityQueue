# Uncomment this line to define a global platform for your project
# platform :ios, "6.0"

target "PriorityQueue" do

end

target "PriorityQueueTests" do
    pod 'Specta', '~> 0.2.1'
    pod 'Expecta', '~> 0.2.4' 
end

